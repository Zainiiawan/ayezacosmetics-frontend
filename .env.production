# ==========================================
# AYEZA COSMETICS — Production (Vercel)
# ==========================================
# NEXT_PUBLIC_* vars are baked into the JS bundle at build time.
# These are ALSO set in Vercel Dashboard → Project → Settings → Environment Variables
# (dashboard values override this file for SSR/ISR at runtime).

# Render backend — do NOT change this
NEXT_PUBLIC_API_URL=https://ayezacosmetics-backend.onrender.com/api

# ⚠️  Actual Vercel URL
NEXT_PUBLIC_APP_URL=https://ayezacosmetics.vercel.app

NEXT_PUBLIC_APP_NAME="AYEZA COSMETICS"
